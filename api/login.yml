info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: https://uni-event-hub.onrender.com/api/auth/login
  body:
    type: json
    data: |
      {
        "email": "test@test.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
